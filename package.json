{
  "name": "focus-reels",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Offline-first healthy reels game for software engineering, AI agents, puzzles, books, travel, and markets.",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "vercel-build": "node -e \"console.log('Focus Reels is a static Vercel app with serverless API routes; no build step required.')\"",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
